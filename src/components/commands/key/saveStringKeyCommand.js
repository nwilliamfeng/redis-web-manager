import { keyActions } from '../../../actions'
import { nodeTypes } from '../../../constants'

/**
 * 修改键命令
 * @param {{key,keyType, dispatch, dbIdx, connectionName, dbId }} param0 
 */
export const saveStringKeyCommand = ({dispatch, key,keyType, dbIdx, connectionName, dbId } )=> {


    alert('aaa');
    // const { dispatch, selectedDbId, selectedConnectionId } = props;
    // const oldKey =formKey.id;
    //  dispatch(keyActions.modifyStringKey(connectionName, dbIdx, dbId,commandHelper.getKeyTypeValue( values.type), values.id,  values.value,oldKey));


}

